# Images
image/*; $HOME/.local/bin/mail-view-attachment %s

# PDFs
application/pdf; $HOME/.local/bin/mail-view-attachment %s

# HTML
text/html; lynx -display_charset=utf-8 -dump %s; nametemplate=%s.html; copiousoutput
